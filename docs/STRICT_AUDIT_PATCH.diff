# Strict Audit Patch — STATUS: ALL APPLIED
#
# This file documented the original audit patches. All patches have been
# applied and verified. This file is retained for historical reference.
#
# Patches applied:
#   1. tsconfig.json — added baseUrl: "."                          [APPLIED]
#   2. lib/db.ts — getDatabaseUrl() validator + datasourceUrl      [APPLIED]
#   3. package.json — prisma seed config                           [APPLIED]
#   4. scripts/test-refresh.ts — prisma.$disconnect()              [APPLIED]
#   5. lib/export.ts — getAppUrl() validator + timeout             [APPLIED]
#   6. app/api/benchmarks/route.ts — runtime, dynamic, try/catch   [APPLIED]
#   7. app/api/refresh/route.ts — runtime, dynamic, try/catch      [APPLIED]
#   8. app/api/export/pdf/route.ts — runtime, dynamic, try/catch   [APPLIED]
#   9. app/api/export/png/route.ts — runtime, dynamic, try/catch   [APPLIED]
#  10. app/export/report/page.tsx — dynamic = "force-dynamic"      [APPLIED]
#
# Additional changes since audit:
#   - prisma/schema.prisma — added dataSource column to BenchmarkResult
#   - app/layout.tsx — added suppressHydrationWarning
#   - 6 new adapter files created (arena multimodal, vellum benchmarks)
#   - lib/types.ts — expanded BenchmarkKey (12 keys) and BenchmarkCategory (10 categories)
#   - lib/weights.ts — redistributed weights for 12 benchmarks
#   - components/benchmark-card.tsx — added live/mock badge, weight indicator, 10 category colors
#   - components/benchmark-table.tsx — added score bars, top-3 highlighting
#   - lib/refresh.ts — getDashboardData expanded for 12 benchmark buckets
#   - lib/mock-data.ts — updated model names with full versions
#
# No further patches from this audit are pending.
